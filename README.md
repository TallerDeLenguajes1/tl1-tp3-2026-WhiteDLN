
# Trabajo Practico N3





### Comandos utiles de GIT

_Recuerda_

```
git cd
git clone
git add .
git commit -m
git push
git origin 
git config --list
git config --global user.name “Su Nombre”
git config --global user.email “su e-mail”
git branch EJEMPLO //CREAR RAMA
git checkout EJEMPLO //SELECCIONAR
Vuelve a la rama main, commitea y 
git merge EJEMPLO

```

